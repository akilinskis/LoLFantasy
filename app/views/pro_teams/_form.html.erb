<%= form_for(@pro_team) do |f| %>
  <% if @pro_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pro_team.errors.count, "error") %> prohibited this pro_team from being saved:</h2>

      <ul>
      <% @pro_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :handle %><br>
    <%= f.text_field :handle %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
