<%= form_for(@global) do |f| %>
  <% if @global.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@global.errors.count, "error") %> prohibited this global from being saved:</h2>

      <ul>
      <% @global.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :current_season %><br>
    <%= f.number_field :current_season %>
  </div>
  <div class="field">
    <%= f.label :current_split %><br>
    <%= f.number_field :current_split %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
